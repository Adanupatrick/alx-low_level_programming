#include <stdio.h>
/**
 * main - Entry point
 *
 * Return Always 0 (Success in alx)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
